---
status: accepted
---

# 使用批次标记划定微信输入窗口

初始化只发送首个基线标记而不回溯历史；普通运行在扫描前通过 Computer Use 发送唯一的最小批次标记，相邻两个标记之间的消息构成该运行的输入窗口，标记之后到达的消息留给下一次运行。任务采用最小任务头、严格相邻的公众号文章卡片和不透明目标标识；第一版隐含单篇来源，每次提交即使内容相同也创建独立任务。微信不接收处理结果、预览链接或错误日志。
