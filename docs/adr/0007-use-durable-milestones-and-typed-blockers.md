---
status: accepted
---

# 使用持久里程碑和类型化阻塞

任务状态只表示最后一个原子提交并通过验证的持久里程碑，正常流程包含任务记录、原始证据、结构化来源、改写产物和外部交付五个语义边界；瞬时执行作为 attempt 进入事件日志。可空、类型化的 blocker 独立表达输入不足、等待重试、重试耗尽和永久失败。运行先登记新增任务，再将历史与新增可执行任务按创建时间处理；单任务故障和有限重试不得中断队列，输入不足任务通过重新提交创建新任务而非原地修改。
