---
status: accepted
---

# 使用文件型运行与任务库

本地数据独立于 Skill 和 Git，并以同级的 `runs` 与 `tasks` 聚合保存：运行记录一次 Agent 执行，任务跨多次运行持续存在，双方通过 ID 和追加式事件关联。任务库使用 JSON Schema、显式状态转换、单写入锁和原子替换；原始证据不可变，结构化来源可重建，已提交改写产物不可变，API 请求与 Markdown 运行报告均为可重建视图。第一版不引入 SQLite、不自动删除、不自动上传备份。
