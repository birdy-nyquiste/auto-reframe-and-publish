---
status: superseded
superseded-by: 0009
---

# 将 Blog 视为外部草稿服务

本项目不建设或部署 Blog，只在真实接口可用后适配其受认证 API。Agent 生成内部 Markdown 与结构化清单，确定性适配器负责目标映射、图片上传、请求构造、验证和幂等交付；客户端只实现草稿创建、草稿更新和必要图片上传，不实现公开发布、删除、用户管理或部署。公开批准仅发生在 Blog 管理后台，任务只有在 API 确认并持久化外部标识后才完成。
